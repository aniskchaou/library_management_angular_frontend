{
  "name": "library-lab-demo",
  "version": "1.0.0",
  "description": "Library Lab demo server — serves the built Angular app + mock API, no backend required.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
